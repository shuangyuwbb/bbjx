<view class="choose-content">
    <view class="choose-picture">
        <image src="{{data.url}}" />
    </view>
    <view class="choose-info">
        <view class="top">
            <p>{{data.text}}</p>
        </view>
        <view class="tip">直降</view>
        <view class="bottom">
            <view class="bottom-price">
                <span>￥{{data.price}}</span>
                <span>{{data.price}}</span>
            </view>
            <view class="btn">立即购买</view>
        </view>
    </view>
</view>